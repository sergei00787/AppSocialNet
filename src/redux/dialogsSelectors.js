export const GetDailogs = (state) => state.DialogState.Dialogs;

export const GetMessages = (state) => state.DialogState.Messages;

export const GetNewMessage = (state) => state.DialogState.NewMessage;