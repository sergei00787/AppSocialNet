export const GetProfile = (state) => state.ProfileState.profile;

export const GetStatus = (state) => state.ProfileState.status;

export const GetPosts = (state) => state.ProfileState.Posts;

export const GetNewPost = (state) => state.ProfileState.NewPost;